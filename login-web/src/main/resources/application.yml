server:
  port: ${PORT:8080}
  servlet:
    session:
      timeout: 15m
      cookie:
        http-only: true
        same-site: lax
spring:
  application:
    name: aaas-login-web
  profiles:
    default: local
ace:
  base-url: ${ACE_BASE_URL:http://localhost:8081}
