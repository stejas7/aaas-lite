# Repository instructions

The repository owner requests that changes be committed and pushed directly to `main` by default. Do not create feature branches or pull requests unless the owner requests them. Respect enforced repository protections and never force-push over existing history.

Use Gradle for this Java/Spring Boot project. Validate relevant code changes before finishing; documentation-only changes do not require repeating the application test suite.
